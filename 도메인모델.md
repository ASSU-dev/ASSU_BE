# Assu 도메인 모델

## 서비스 개요

Assu는 숭실대학교 학생, 학생회(관리자), 제휴업체, 운영자가 참여하는 교내 제휴 혜택 플랫폼이다.
- 학생회(ADMIN)는 제휴업체(PARTNER)와 제휴 계약을 체결한다.
- 학생(STUDENT)은 QR 스캔으로 제휴 혜택을 사용한다.
- 백오피스(BACKOFFICE)는 회원 관리, 신고 처리, 알림 발송 등 운영 업무를 담당한다.

---

## [ 회원 어그리거트 ]

### 회원 (Member)
_Aggregate Root_

#### 속성
- `id`: `Long`
- `role`: `UserRole` — 회원 역할 (STUDENT, ADMIN, PARTNER, BACKOFFICE)
- `isActivated`: `ActivationStatus` — 계정 상태 (ACTIVE, INACTIVE, SUSPEND)
- `isLocationTermAgreed`: `Boolean` — 위치 정보 동의
- `isMarketingTermAgreed`: `Boolean` — 마케팅 동의
- `profileUrl`: `String` — 프로필 이미지 S3 URL
- `chatBlocked`: `Boolean` — 채팅 차단 여부 (기본값: false)
- `deletedAt`: `LocalDateTime` — 소프트 삭제 시점

#### 연관관계
- `studentProfile`: `Student` (1:1)
- `adminProfile`: `Admin` (1:1)
- `partnerProfile`: `Partner` (1:1)
- `backofficeProfile`: `BackofficeUser` (1:1)
- `ssuAuth`: `SSUAuth` (1:1) — 학생 유세인트 인증
- `commonAuth`: `CommonAuth` (1:1) — 이메일/비밀번호 인증

#### 행위
- `resolveName()`: 역할별 이름(이름/업체명/단체명) 반환
- `setProfile(Object)`: 역할별 프로필 엔티티 연결

#### 규칙
- 역할에 따라 하나의 프로필(Student/Admin/Partner/BackofficeUser)만 가진다.
- 소프트 삭제 지원 (`deletedAt` 설정).
- 계정 상태가 SUSPEND이면 로그인 불가.

---

### 학생 (Student)
_Entity_

#### 속성
- `id`: `Long` (= member_id)
- `name`: `String` — 이름
- `studentNumber`: `String` — 학번
- `phoneNumber`: `String` — 전화번호
- `enrollmentStatus`: `EnrollmentStatus` — 재학 상태 (ENROLLED, ON_LEAVE, GRADUATED 등)
- `department`: `Department` — 학부
- `major`: `Major` — 전공

#### 규칙
- 숭실대 유세인트 토큰(`SSUAuth`)으로만 가입 가능.

---

### 관리자 (Admin) — 학생회
_Entity_

#### 속성
- `id`: `Long` (= member_id)
- `name`: `String` — 학생회 단체명
- `phoneNumber`: `String` — 연락처

#### 규칙
- SUSPEND 상태로 가입 → 백오피스 승인 후 ACTIVE.

---

### 제휴업체 (Partner)
_Entity_

#### 속성
- `id`: `Long` (= member_id)
- `name`: `String` — 업체명
- `phoneNumber`: `String` — 연락처

#### 연관관계
- `store`: `Store` (1:1)

#### 규칙
- SUSPEND 상태로 가입 → 백오피스 승인 후 ACTIVE.

---

### 백오피스 사용자 (BackofficeUser)
_Entity_

#### 속성
- `id`: `Long` (= member_id)
- `name`: `String` — 운영자 이름

#### 규칙
- `/auth/backoffice/login`으로만 로그인 가능.
- `aud=backoffice` 클레임이 있는 별도 JWT 발급.

---

### 공통 인증 (CommonAuth) — 이메일/비밀번호
_Entity_

#### 속성
- `id`: `Long` (= member_id)
- `email`: `String` — 이메일 (unique)
- `hashedPassword`: `String` — 암호화된 비밀번호
- `lastLoginAt`: `LocalDateTime` — 마지막 로그인 시각

---

### 유세인트 인증 (SSUAuth)
_Entity_

#### 속성
- `id`: `Long` (= member_id)
- `studentNumber`: `String` — 유세인트 학번 (unique)

#### 규칙
- 숭실대 유세인트 API 토큰 검증으로 학생 신원 확인.

---

## [ 가게 어그리거트 ]

### 가게 (Store)
_Aggregate Root_

#### 속성
- `id`: `Long`
- `name`: `String` — 가게명
- `address`: `String` — 도로명 주소
- `detailAddress`: `String` — 상세 주소
- `latitude`: `double` — 위도
- `longitude`: `double` — 경도
- `point`: `Point` — 공간 좌표 (Hibernate Spatial)
- `rate`: `Integer` — 평균 평점 (0~5)
- `isActivate`: `ActivationStatus` — 영업 상태

#### 연관관계
- `partner`: `Partner` (N:1)

#### 행위
- `linkPartner(Partner)`: 제휴업체 연결
- `setGeo(lat, lng, point)`: 좌표 설정

---

## [ 제휴 어그리거트 ]

### 제휴 계약서 (Paper)
_Aggregate Root_

#### 속성
- `id`: `Long`
- `partnershipPeriodStart`: `LocalDate` — 제휴 시작일
- `partnershipPeriodEnd`: `LocalDate` — 제휴 종료일
- `isActivated`: `ActivationStatus` — 계약 상태 (ACTIVE, INACTIVE, SUSPEND)
- `contractImageKey`: `String` — 계약서 이미지 S3 키

#### 연관관계
- `admin`: `Admin` (N:1)
- `partner`: `Partner` (N:1)
- `store`: `Store` (N:1)

#### 행위
- `updateContractImageKey(String)`: 계약서 이미지 키 변경

#### 규칙
- `ACTIVE` 상태일 때만 학생이 QR 스캔으로 혜택 사용 가능.
- `PaperScheduler`가 만료된 Paper를 자동으로 `INACTIVE`로 변경.

---

### 계약서 내용 (PaperContent)
_Entity_

#### 속성
- `id`: `Long`
- `title`: `String` — 혜택 제목
- `description`: `String` — 혜택 설명
- `criterionType`: `CriterionType` — 기준 유형 (AMOUNT, COUNT 등)
- `criterionValue`: `Integer` — 기준값

#### 연관관계
- `paper`: `Paper` (N:1)
- `goods`: `List<Goods>` (1:N)

---

### 제공 상품/혜택 (Goods)
_Entity_

#### 속성
- `id`: `Long`
- `name`: `String` — 상품명
- `optionType`: `OptionType` — 옵션 유형 (DISCOUNT, FREE 등)
- `optionValue`: `Integer` — 옵션값

#### 연관관계
- `paperContent`: `PaperContent` (N:1)

---

## [ QR 인증 어그리거트 ]

### QR 인증 (QRCertification)
_Aggregate Root_

#### 속성
- `id`: `Long`
- `certifiedAt`: `LocalDateTime` — 인증 시각

#### 연관관계
- `student`: `Student` (N:1)
- `paper`: `Paper` (N:1)

---

### 그룹 인증 (AssociateCertification)
_Entity_

#### 속성
- `id`: `Long`
- `sessionStatus`: `SessionStatus` — 세션 상태 (OPEN, CLOSED)
- `sessionCode`: `String` — 그룹 세션 코드

#### 규칙
- WebSocket(`/app/certify`)으로 그룹원 실시간 인증.

---

### 임시 QR (Qr)
_Entity_

#### 속성
- `id`: `Long`
- `qrToken`: `String` — QR 토큰 (UUID)
- `expiredAt`: `LocalDateTime` — 만료 시각

#### 규칙
- 학생이 스탬프 이벤트 등에 사용하는 단기 QR.

---

## [ 채팅 어그리거트 ]

### 채팅방 (ChattingRoom)
_Aggregate Root_

#### 속성
- `id`: `Long`
- `lastMessage`: `String` — 마지막 메시지 내용
- `lastMessageAt`: `LocalDateTime` — 마지막 메시지 시각

#### 연관관계
- `admin`: `Admin` (N:1)
- `partner`: `Partner` (N:1)

---

### 메시지 (Message)
_Entity_

#### 속성
- `id`: `Long`
- `content`: `String` — 메시지 내용
- `messageType`: `MessageType` — 메시지 유형 (TEXT, IMAGE)
- `senderId`: `Long` — 발신자 Member ID
- `isRead`: `Boolean` — 읽음 여부

#### 연관관계
- `chattingRoom`: `ChattingRoom` (N:1)

---

### 차단 (Block)
_Entity_

#### 속성
- `id`: `Long`

#### 연관관계
- `blocker`: `Member` (N:1)
- `blocked`: `Member` (N:1)

---

## [ 알림 어그리거트 ]

### 알림 (Notification)
_Aggregate Root_

#### 속성
- `id`: `Long`
- `title`: `String` — 알림 제목
- `body`: `String` — 알림 내용
- `isRead`: `Boolean` — 읽음 여부
- `notificationType`: `NotificationType` — 알림 유형

#### 연관관계
- `member`: `Member` (N:1)

---

### 알림 발행 큐 (NotificationOutbox)
_Entity_

#### 속성
- `id`: `Long`
- `payload`: `String` — FCM 메시지 JSON
- `status`: `OutboxStatus` — 발행 상태 (PENDING, PUBLISHED, FAILED)
- `retryCount`: `Integer` — 재시도 횟수 (최대 3회)
- `failedAt`: `LocalDateTime` — 실패 시각

#### 규칙
- Outbox 패턴: 트랜잭션 커밋 후 `OutboxAfterCommitPublisher` → RabbitMQ → FCM.
- 실패 시 `OutboxRetryScheduler`가 최대 3회 재시도.

---

## [ 리뷰 어그리거트 ]

### 리뷰 (Review)
_Aggregate Root_

#### 속성
- `id`: `Long`
- `content`: `String` — 리뷰 내용
- `rate`: `Integer` — 별점 (1~5)

#### 연관관계
- `student`: `Student` (N:1)
- `store`: `Store` (N:1)
- `reviewPhotos`: `List<ReviewPhoto>` (1:N)

---

### 리뷰 사진 (ReviewPhoto)
_Entity_

#### 속성
- `id`: `Long`
- `imageKey`: `String` — S3 이미지 키

#### 연관관계
- `review`: `Review` (N:1)

---

## [ 신고 어그리거트 ]

### 신고 (Report)
_Aggregate Root_

#### 속성
- `id`: `Long`
- `reportType`: `ReportType` — 신고 유형
- `reportTargetType`: `ReportTargetType` — 신고 대상 유형 (REVIEW, CHAT)
- `targetId`: `Long` — 신고 대상 ID
- `status`: `ReportStatus` — 처리 상태 (PENDING, RESOLVED)

#### 연관관계
- `reporter`: `Member` (N:1)

---

## [ 제안 어그리거트 ]

### 제안 (Suggestion)
_Aggregate Root_

#### 속성
- `id`: `Long`
- `content`: `String` — 제안 내용
- `storeName`: `String` — 제안 업체명

#### 연관관계
- `student`: `Student` (N:1)
- `admin`: `Admin` (N:1)

---

## [ 문의 어그리거트 ]

### 문의 (Inquiry)
_Aggregate Root_

#### 속성
- `id`: `Long`
- `title`: `String` — 문의 제목
- `content`: `String` — 문의 내용
- `answer`: `String` — 백오피스 답변

#### 연관관계
- `member`: `Member` (N:1)

---

## [ 기타 엔티티 ]

### FCM 디바이스 토큰 (DeviceToken)

#### 속성
- `id`: `Long`
- `token`: `String` — FCM 디바이스 토큰

#### 연관관계
- `member`: `Member` (N:1)

---

### 앱 리뷰 (AppReview)

#### 속성
- `id`: `Long`
- `isReviewed`: `Boolean` — 앱 스토어 리뷰 완료 여부

#### 연관관계
- `student`: `Student` (1:1)

---

### 백오피스 감사 로그 (BackofficeAuditLog)

#### 속성
- `id`: `Long`
- `action`: `String` — 수행 액션 (예: MEMBER_APPROVE)
- `targetId`: `String` — 대상 ID
- `status`: `BackofficeAuditStatus` — 처리 결과 (SUCCESS, FAILURE)
- `performedAt`: `LocalDateTime` — 수행 시각

#### 연관관계
- `backofficeUser`: `BackofficeUser` (N:1)

---

## 공통 엔티티

### BaseEntity

모든 도메인 엔티티가 상속하는 공통 엔티티.

#### 속성
- `createdAt`: `LocalDateTime` — 생성 시각
- `updatedAt`: `LocalDateTime` — 최종 수정 시각
