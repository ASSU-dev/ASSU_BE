# Assu 용어 사전

| 한국어 | 영어 | 설명 |
|--------|------|------|
| 학생 | Student | 숭실대학교 재학생. 유세인트 토큰으로 인증하며 QR 스캔으로 제휴 혜택을 사용한다. `UserRole.STUDENT`를 가진 회원. |
| 학생회 / 관리자 | Admin | 학생회 단체 계정. `SUSPEND` 상태로 가입 후 백오피스 승인 필요. 제휴업체와 계약을 체결하고 채팅으로 소통한다. `UserRole.ADMIN`. |
| 제휴업체 | Partner | 학생 혜택을 제공하는 업체 계정. `SUSPEND` 상태로 가입 후 백오피스 승인 필요. `UserRole.PARTNER`. |
| 운영자 | BackofficeUser | 백오피스 운영자 계정. `/auth/backoffice/login`으로 로그인하며 `aud=backoffice` JWT를 발급받는다. `UserRole.BACKOFFICE`. |
| 회원 | Member | 학생/학생회/제휴업체/운영자가 공유하는 공통 계정 엔티티. 역할에 따라 하위 프로필 엔티티와 1:1 연관. |
| 계정 상태 | ActivationStatus | 회원 계정의 활성화 상태. `ACTIVE`(정상), `INACTIVE`(비활성/거절), `SUSPEND`(승인 대기) |
| 가게 | Store | 제휴업체가 운영하는 매장. 공간 좌표(Point)를 가지며 지도 검색에 활용. |
| 제휴 계약서 | Paper | 학생회와 제휴업체 간의 제휴 계약. 계약 기간과 혜택 내용을 포함. |
| 계약서 내용 | PaperContent | Paper 내에 포함된 혜택 항목. 기준 조건(CriterionType)과 제공 상품(Goods)으로 구성. |
| 제공 상품 | Goods | PaperContent에 포함된 실제 혜택 항목. 옵션 유형(OptionType)과 값으로 정의. |
| 기준 유형 | CriterionType | 혜택 사용 기준 유형. 금액 기준(`AMOUNT`), 횟수 기준(`COUNT`) 등. |
| 옵션 유형 | OptionType | 혜택 제공 방식. 할인(`DISCOUNT`), 무료 제공(`FREE`) 등. |
| 제휴 사용 기록 | PartnershipUsage | 학생이 특정 Paper의 혜택을 사용한 이력. |
| QR 인증 | QRCertification | 학생이 QR 스캔으로 제휴 혜택을 인증한 기록. |
| 그룹 인증 | AssociateCertification | 여러 학생이 하나의 세션에서 동시에 QR 인증하는 방식. WebSocket 기반. |
| 임시 QR | Qr (TemporaryQr) | 스탬프 이벤트 등에 사용하는 단기 QR 토큰. |
| 알림 발행 큐 | NotificationOutbox | Outbox 패턴으로 알림을 안전하게 발행하기 위한 임시 저장 엔티티. 트랜잭션 커밋 후 RabbitMQ로 전달. |
| 알림 유형 | NotificationType | FCM 푸시 알림의 종류. 채팅, 인증 완료, 제휴 상태 변경 등. |
| 재학 상태 | EnrollmentStatus | 학생의 학적 상태. 재학, 휴학, 졸업 등. |
| 채팅방 | ChattingRoom | 학생회(Admin)와 제휴업체(Partner) 간 1:1 채팅 채널. |
| 제안 | Suggestion | 학생이 학생회에게 신규 제휴 업체를 건의하는 기능. |
| 문의 | Inquiry | 학생이 운영팀에 질문하는 1:1 문의. 백오피스에서 답변. |
| 신고 | Report | 리뷰 또는 채팅 메시지에 대한 신고. 백오피스에서 처리. |
| 신고 대상 유형 | ReportTargetType | 신고 대상의 종류. 리뷰(`REVIEW`), 채팅(`CHAT`). |
| 백오피스 감사 로그 | BackofficeAuditLog | 운영자의 CUD 액션을 기록하는 감사 이력. `@BackofficeAudited` AOP로 자동 기록. |
| 유세인트 | USaint | 숭실대학교 학생 포털 서비스. 학생 인증 시 USaint 토큰을 발급받아 검증. |
| 소프트 삭제 | Soft Delete | `deletedAt` 필드에 삭제 시각을 기록하여 DB에서 실제로 삭제하지 않는 방식. |
| 토큰 회전 | Token Rotation | Refresh Token을 사용할 때마다 새 Refresh Token을 발급하고 기존 토큰을 무효화하는 방식. |
| 블랙리스트 | Blacklist | 로그아웃된 Access Token을 Redis에 등록하여 재사용을 방지하는 목록. |
| Outbox 패턴 | Outbox Pattern | 알림 발송 시 DB에 먼저 기록한 뒤 트랜잭션 커밋 후 메시지 브로커로 발행하는 패턴. 알림 유실 방지. |
| FCM | Firebase Cloud Messaging | Google Firebase의 푸시 알림 서비스. RabbitMQ Consumer가 FCM으로 실제 발송. |
| 공간 데이터 | Spatial Data | 가게의 위치 좌표(`Point`)를 저장·조회하는 기능. Hibernate Spatial + MariaDB 사용. |
