# Assu 개발 가이드

## 아키텍처

계층형 DDD(Domain-Driven Design) 아키텍처를 사용한다.

```text
Presentation  (Controller)   ← 클라이언트 요청 수신, DTO 반환
      ↓
Application   (Service)      ← 비즈니스 로직, 트랜잭션
      ↓
Domain        (Entity, Repository interface)
      ↑
Infrastructure (Repository impl, External API client)
```

**규칙**
- 상위 레이어 → 하위 레이어 접근 가능, 역방향 불가
- 각 도메인은 서로 철저히 분리
- 각 레이어는 하나의 관심사에만 집중

---

## 패키지 구조

```text
src/main/java/com/assu/server/
├── domain/          # 도메인별 패키지
│   └── <domain>/
│       ├── controller/
│       ├── service/
│       ├── repository/
│       ├── dto/
│       ├── entity/
│       │   └── enums/
│       └── exception/
│           ├── handler/
│           ├── validator/
│           └── annotation/
├── global/          # 공통 인프라
│   ├── apiPayload/
│   ├── config/
│   ├── exception/
│   ├── security/
│   └── util/
└── infra/           # 외부 서비스 클라이언트
    ├── aligo/
    ├── firebase/
    └── s3/
```

---

## 네이밍 규칙

### DTO
- 클래스명: `~~DTO` (`UserResponseDTO`, `LoginRequestDTO`)
- 항상 `record`로 선언
- 변환은 DTO 내부 `static` 팩토리 메서드 (`from`, `of`)

### Service
- 인터페이스: `~~Service`
- 구현체: `~~ServiceImpl`
- 읽기/쓰기가 명확히 분리되는 경우 `~~QueryService` / `~~CommandService`로 분리 가능

### Repository
- `~~Repository` (JPA)

### Enum
- 도메인별: `<domain>/entity/enums/`
- 공통: `domain/common/entity/enums/` 또는 `domain/common/enums/`

### 필드명
의도가 드러나게 작성한다.

| 나쁜 예 | 좋은 예 |
|---------|---------|
| `password` | `hashedPassword` |
| `status` | `activationStatus` |
| `flag` | `isChatBlocked` |

### 조회 메서드
- Optional/null 반환 가능: `findById`, `findByEmail`
- 없으면 예외: `findByIdOrThrow`

---

## 코드 작성 유의사항

### Entity
- 무분별한 public setter 금지. 상태 변경은 행위 메서드로 표현.
  - 예: `member.activate()`, `paper.expire()`
- 소프트 삭제는 `deletedAt` 필드에 시각 기록.

### Service
- `@Transactional`은 **구현 클래스 레벨**에 선언.
- 읽기 전용 서비스: `@Transactional(readOnly = true)` 클래스에 선언.
- 쓰기 메서드에만 `@Transactional` 별도 선언.

### Repository
- 쿼리 내에 `lower()` 등 서비스 로직 삽입 금지.
- N+1 문제 주의. 필요 시 `@EntityGraph`, `fetch join` 활용.
- 복잡한 조회는 JPQL보다 QueryDSL 고려.

### DTO
- 모든 DTO는 `record`로 선언.
- 모든 필드에 `@Schema(description = "...", example = "...")` 작성.
- `@NotNull`, `@NotBlank`, `@Size`, `@Email` 등 제약 조건 명시.

### 예외 처리
- `ErrorStatus` enum에 HTTP 상태 + 커스텀 코드 + 메시지 정의.
- `GeneralException(ErrorStatus)` throw → `GlobalExceptionAdvice`에서 일괄 처리.
- 비즈니스 예외 vs 시스템 예외를 명확히 구분.

---

## API 응답 규칙

- 모든 API는 `BaseResponse<T>` 래퍼로 응답.
- 조회 API: 데이터 반환.
- 생성 API: 생성된 리소스 ID 또는 핵심 결과 반환.
- 수정/삭제 API: 빈 result로 성공 응답.

---

## API URL 컨벤션

### 기본 원칙

- URL은 **소문자 + 하이픈(kebab-case)** 으로 작성한다. (`device-tokens`, `temporary-qr`)
- 리소스는 **복수 명사**로 표현한다. (`/reviews`, `/stores`, `/notifications`)
- URL에 **동사를 사용하지 않는다**. 행위는 HTTP 메서드로 표현한다.
- 계층 관계는 경로로 표현한다. (`/stores/{storeId}/reviews`)
- 쿼리 파라미터는 필터링·정렬·페이지네이션에만 사용한다.

### HTTP 메서드 사용 원칙

| 메서드 | 용도 | 예시 |
|--------|------|------|
| `GET` | 리소스 단건/목록 조회 | `GET /reviews/{reviewId}` |
| `POST` | 리소스 생성 | `POST /reviews` |
| `PATCH` | 리소스 부분 수정 | `PATCH /reviews/{reviewId}` |
| `DELETE` | 리소스 삭제 | `DELETE /reviews/{reviewId}` |
| `PUT` | 리소스 전체 교체 (지양) | — |

> `PUT`은 전체 교체 의미라 부분 수정에는 `PATCH`를 사용한다.

### 경로 변수 vs 쿼리 파라미터

| 구분 | 사용 시점 | 예시 |
|------|-----------|------|
| 경로 변수 `{id}` | 특정 리소스를 식별할 때 | `/stores/{storeId}` |
| 쿼리 파라미터 `?key=value` | 필터링, 정렬, 페이지네이션 | `/stores?page=0&size=10` |

### URL 역할 접두사

역할 전용 API는 접두사로 명확히 구분한다.

| 접두사 | 대상 |
|--------|------|
| `/auth/**` | 인증/인가 (퍼블릭) |
| `/backoffice/**` | 운영자 전용 |
| `/admin/**` | 학생회 전용 |
| `/partner/**` | 제휴업체 전용 |
| `/students/**` | 학생 전용 |

### 잘못된 예 vs 올바른 예

| 나쁜 예 | 좋은 예 | 이유 |
|---------|---------|------|
| `POST /createReview` | `POST /reviews` | URL에 동사 사용 금지 |
| `GET /getStore/1` | `GET /stores/1` | 동사 + 단수 명사 |
| `DELETE /review_delete?id=1` | `DELETE /reviews/{reviewId}` | 언더스코어, 동사, 쿼리 파라미터 오용 |
| `PATCH /stores/updateName` | `PATCH /stores/{storeId}` | URL에 행위 표현 금지 |

---

## Swagger 작성 규칙

- OpenAPI 3 (`springdoc-openapi`) 기준 어노테이션 사용.
- 모든 컨트롤러 메서드: `@Operation(summary = "...", description = "...")`.
- 모든 DTO 필드: `@Schema(description = "...", example = "...")`.

### @Operation 작성 포맷

컨트롤러의 `@Operation` 어노테이션은 항상 다음 구조를 따른다.

```java
@Operation(
    summary = "{간결한 API 요약}",
    description = "# [{버전}]({노션/기획 문서 링크})\n" +
            "- {주요 기능/특징 1}\n" +
            "- {주요 기능/특징 2}\n" +
            "- {정책 및 제한사항}\n" +
            "\n**Request Body:**\n" +
            "  - `{필드명}` ({타입}, {필수 여부}): {설명}\n" +
            "\n**Response:**\n" +
            "  - {성공 시 응답 형태}"
)
```

**규칙:**
- `summary`: 한 줄로 API 동작을 간결하게 표현.
- `description`: 마크다운 형식으로 작성. 버전 헤더 → 주요 기능 → Request Body → Response 순서를 유지.
- Request Body가 없는 GET 요청은 `**Request Body:**` 항목 생략.
- 노션/기획 문서 링크가 없으면 버전 헤더만 텍스트로 작성 (`# v1.0` 형식).
- 컨트롤러 코드 작성/수정 시 반드시 이 포맷을 함께 업데이트한다.

---

## 코드 품질

- 미사용 import, 로그, 메서드, 파일은 모두 제거 (IntelliJ: `Alt + Ctrl + O`).
- 불필요한 주석 제거. WHY가 명확하지 않은 경우에만 한 줄 주석 허용.
- WHAT을 설명하는 주석은 작성하지 않는다 (코드 자체가 표현해야 함).
- 미결 사항은 `// FIXME: ...` 형식으로 표시.

---

## 로컬 실행

```bash
./gradlew bootRun --args='--spring.profiles.active=local'
```

설정 파일은 `config/local/` 디렉토리에 위치. `processResources` 단계에서 `src/main/resources`로 복사.

---

## 테스트

- 테스트 메서드명: **영어**로 작성.
- given/when/then 흐름이 명확한 경우 주석으로 구분.
- 테스트 DB: H2 인메모리 (`src/test/resources/application-test.yml`).

```bash
./gradlew test
./gradlew test --tests "com.assu.server.backoffice.BackofficeAuditAspectTest"
```
